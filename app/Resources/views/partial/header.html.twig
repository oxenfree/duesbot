<nav>
    <div class="nav-wrapper cyan darken-2">
        <a href="{{ path('app_index') }}" class="brand-logo"><i class="material-icons">start</i>Duesbot</a>
        <a href="#" data-activates="mobile-demo" class="button-collapse"><i class="material-icons">menu</i></a>
        <ul id="nav-mobile" class="right hide-on-med-and-down">
            {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                <li>
                    <i class="small material-icons">perm_identity</i>
                </li>
                <li>
                    <a href="{{ path('app_profile_show') }}">Profile</a>
                </li>
                {% if is_granted('ROLE_ADMIN') %}
                    <li><a href="{{ path('app_admin_index') }}">Admin</a></li>
                {% endif %}
                <li><a href="/logout">Logout</a></li>
            {% else %}
                <li class="cyan darken-3"><a href="/register" class="white-text">Register</a></li>
            {% endif %}
        </ul>
        <ul class="side-nav" id="mobile-demo">
            {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                <li>
                    <i class="small material-icons">perm_identity</i>
                </li>
                <li>
                    <a href="{{ path('app_profile_show') }}">Profile</a>
                </li>
                {% if is_granted('ROLE_ADMIN') %}
                    <li><a href="{{ path('app_admin_index') }}">Admin</a></li>
                {% endif %}
                <li><a href="/logout">Logout</a></li>
            {% else %}
                <li class="cyan darken-3"><a href="/register" class="white-text">Register</a></li>
            {% endif %}
        </ul>
    </div>
</nav>